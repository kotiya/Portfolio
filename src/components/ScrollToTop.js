import { useEffect } from "react";
import { useLocation } from "react-router-dom";

function ScrollToTop() {
  const { pathname } = useLocation();
  useEffect(() => {
    window.scrollTo(0, 0);
  }, [pathname]);
  return null;
}

export default ScrollToTop;
import { useEffect } from "react";useEffectuseEffectuseEffect"react"import { useLocation } from "react-router-dom";useLocationuseLocationuseLocation"react-router-dom"function ScrollToTop() {
  const { pathname } = useLocation();
  useEffect(() => {
    window.scrollTo(0, 0);
  }, [pathname]);
  return null;
}ScrollToTop{
  const { pathname } = useLocation();
  useEffect(() => {
    window.scrollTo(0, 0);
  }, [pathname]);
  return null;
}const { pathname } = useLocation();{ pathname } = useLocation(){ pathname }pathnamepathnamepathnameuseLocation()useLocationuseEffect(() => {
    window.scrollTo(0, 0);
  }, [pathname]);useEffect(() => {
    window.scrollTo(0, 0);
  }, [pathname])useEffect() => {
    window.scrollTo(0, 0);
  }{
    window.scrollTo(0, 0);
  }window.scrollTo(0, 0);window.scrollTo(0, 0)window.scrollTowindowscrollTo00[pathname]pathnamereturn null;nullexport default ScrollToTop;ScrollToTop