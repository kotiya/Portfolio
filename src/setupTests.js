// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom'; // Importing the jest-dom library for custom matchers

// The above import statement adds custom matchers to Jest for asserting on DOM nodes.
// For example, it provides the `toHaveTextContent` matcher that allows you to assert
// whether an element has a specific text content.

// The library is useful when writing tests for React components or any other code that
// manipulates the DOM. It enhances the capabilities of Jest's default matchers.

// You can learn more about jest-dom and its available matchers by visiting the
// official GitHub repository: https://github.com/testing-library/jest-dom
